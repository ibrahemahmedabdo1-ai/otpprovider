import {requireRoles} from '@/lib/session'; import {db} from '@/lib/prisma'; export default async function Sales(){await requireRoles(['SUPER_ADMIN','ADMIN','SALES']); const users=await db.user.findMany({where:{role:'CUSTOMER'},orderBy:{createdAt:'desc'},take:100}); return <div className="dash"><aside className="side"><a href="/admin">← الإدارة</a></aside><section className="content"><h1>Sales CRM</h1><div className="kpis"><div className="kpi">Leads<strong>{users.length}</strong></div><div className="kpi">Customers<strong>{users.length}</strong></div></div><div className="card" style={{marginTop:20}}><table className="table"><thead><tr><th>Customer</th><th>Created</th><th>Credits</th><th>Test</th></tr></thead><tbody>{users.map(x=><tr key={x.id}><td>{x.name}<br/>{x.email}</td><td>{x.createdAt.toLocaleDateString('en-GB')}</td><td>{x.credits}</td><td>{x.testCredits}</td></tr>)}</tbody></table></div></section></div>}