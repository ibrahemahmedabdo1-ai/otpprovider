import {db} from '@/lib/prisma'; import {requireRoles} from '@/lib/session'; import {runHealthScan,repairOpenErrors} from '@/lib/actions'; export default async function Errors(){await requireRoles(['SUPER_ADMIN','ADMIN','SUPPORT']); const errors=await db.errorEvent.findMany({where:{status:'OPEN'},orderBy:{createdAt:'desc'}}); return <div className="dash"><aside className="side"><a href="/admin">← الإدارة</a></aside><section className="content"><div className="top"><div><h1>System Error Center</h1><p className="muted">فحص قاعدة البيانات والوحدات الأساسية وتسجيل الأخطاء.</p></div><form action={async()=>{'use server';await runHealthScan()}}><button className="btn">🔍 اكتشاف الأخطاء</button></form></div><form action={async()=>{'use server';await repairOpenErrors()}}><button className="btn alt">🔧 أصلح الآن</button></form><div style={{marginTop:20}}>{errors.length===0?<div className="notice success">لا توجد أخطاء مفتوحة حالياً.</div>:errors.map(e=><div className="notice danger" key={e.id}><b>{e.severity} · {e.source}</b><br/>{e.message}<br/><small>{e.createdAt.toLocaleString('en-GB')}</small></div>)}</div></section></div>}