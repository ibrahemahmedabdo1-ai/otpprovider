import {db} from '@/lib/prisma'; import {requireRoles} from '@/lib/session'; import {createStaff,generateTestCredits,updateUser,deleteUser,resetUserPassword} from '@/lib/actions'; export default async function Users(){await requireRoles(['SUPER_ADMIN','ADMIN']); const users=await db.user.findMany({orderBy:{createdAt:'desc'}}); return <div className="dash"><aside className="side"><a href="/admin">← الإدارة</a></aside><section className="content"><h1>المستخدمون والموظفون</h1><div className="grid"><div className="card"><h2>إنشاء حساب موظف</h2><form action={createStaff}><input name="name" placeholder="الاسم" required/><input name="email" type="email" placeholder="Email" required/><input name="password" type="password" placeholder="Temporary password" minLength={10} required/><select name="role" defaultValue="SUPPORT"><option>ADMIN</option><option>SUPPORT</option><option>SALES</option><option>MARKETING</option></select><button className="btn">إنشاء</button></form></div><div className="card"><h2>توليد Test Credits</h2><form action={generateTestCredits}><select name="userId">{users.filter(x=>x.role==='CUSTOMER').map(x=><option key={x.id} value={x.id}>{x.email}</option>)}</select><input name="amount" type="number" min="1" placeholder="عدد الرصيد" required/><button className="btn">توليد</button></form></div></div><div className="card" style={{marginTop:20}}><table className="table"><thead><tr><th>المستخدم</th><th>الدور</th><th>الحالة</th><th>الرصيد</th><th>Test</th><th>تاريخ الإنشاء</th><th>إدارة</th></tr></thead><tbody>{users.map(x=><tr key={x.id}><td>{x.name}<br/><span className="muted">{x.email}</span></td><td>{x.role}</td><td>{x.status}</td><td>{x.credits}</td><td>{x.testCredits}</td><td>{x.createdAt.toLocaleString('en-GB')}</td><td><details><summary>إدارة</summary><form action={updateUser}><input type="hidden" name="userId" value={x.id}/><input name="name" defaultValue={x.name}/><select name="role" defaultValue={x.role}><option>ADMIN</option><option>SUPPORT</option><option>SALES</option><option>MARKETING</option><option>CUSTOMER</option></select><select name="status" defaultValue={x.status}><option>ACTIVE</option><option>SUSPENDED</option><option>PENDING</option></select><button className="btn">حفظ</button></form><form action={resetUserPassword}><input type="hidden" name="userId" value={x.id}/><input name="newPassword" type="password" minLength={10} placeholder="كلمة مرور جديدة" required/><button className="btn alt">إعادة تعيين</button></form>{x.role!=='SUPER_ADMIN'&&<form action={deleteUser}><input type="hidden" name="userId" value={x.id}/><button className="btn alt">حذف</button></form>}</details></td></tr>)}</tbody></table></div></section></div>}