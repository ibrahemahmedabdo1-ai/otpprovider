import {db} from '@/lib/prisma'; import {requireRoles} from '@/lib/session'; import {createMeeting} from '@/lib/actions'; export default async function Meetings(){await requireRoles(['SUPER_ADMIN','ADMIN']); const users=await db.user.findMany({where:{status:'ACTIVE'},orderBy:{name:'asc'}}); const meetings=await db.meeting.findMany({orderBy:{startsAt:'desc'},include:{participants:{include:{user:true}}}}); return <div className="dash"><aside className="side"><a href="/admin">← الإدارة</a></aside><section className="content"><h1>اجتماعات الإدارة</h1><div className="card"><form action={createMeeting}><input name="title" placeholder="عنوان الاجتماع" required/><textarea name="description" placeholder="الوصف"/><input name="startsAt" type="datetime-local" required/><input name="endsAt" type="datetime-local" required/><input name="location" placeholder="المكان"/><input name="meetingUrl" placeholder="رابط الاجتماع"/><select name="participantIds" multiple>{users.map(x=><option key={x.id} value={x.id}>{x.name} — {x.role}</option>)}</select><button className="btn">إنشاء الاجتماع</button></form></div><div className="grid" style={{marginTop:20}}>{meetings.map(m=><div className="card" key={m.id}><h3>{m.title}</h3><p className="muted">{m.startsAt.toLocaleString('en-GB')} → {m.endsAt.toLocaleString('en-GB')}</p><p>{m.location}</p><p className="muted">{m.participants.length} مشارك</p></div>)}</div></section></div>}