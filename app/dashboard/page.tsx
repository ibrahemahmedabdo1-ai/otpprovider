import Link from 'next/link'; import {getUser} from '@/lib/session'; import {db} from '@/lib/prisma'; export default async function Dashboard(){const u=await getUser(); if(!u)return <main className="auth"><Link className="btn" href="/login">تسجيل الدخول</Link></main>; const [logs,tickets]=await Promise.all([db.otpLog.findMany({where:{userId:u.id},orderBy:{createdAt:'desc'},take:10}),db.ticket.findMany({where:{customerId:u.id},orderBy:{createdAt:'desc'},take:5})]); const staff=['SUPER_ADMIN','ADMIN','SUPPORT','SALES','MARKETING'].includes(u.role); return <div className="dash"><aside className="side"><div className="brand">OTP<span>Provider</span></div><a href="/dashboard">الرئيسية</a><a href="/tickets">التذاكر</a><a href="/delivery">تسليم الرسائل</a><a href="/settings">الإعدادات</a>{staff&&<a href="/admin">لوحة الإدارة</a>}</aside><section className="content"><div className="top"><div><h1>مرحباً {u.name}</h1><p className="muted">{u.role}</p></div><Link className="btn" href="/errors">فحص النظام</Link></div><div className="kpis"><div className="kpi">الرصيد<strong>{u.credits}</strong></div><div className="kpi">Test Credits<strong>{u.testCredits}</strong></div><div className="kpi">طلبات OTP<strong>{logs.length}</strong></div><div className="kpi">التذاكر<strong>{tickets.length}</strong></div></div><div className="section" style={{padding:'30px 0'}}><div className="card"><h2>آخر عمليات التسليم</h2><table className="table"><tbody>{logs.map(l=><tr key={l.id}><td>{l.service}</td><td>{l.recipient}</td><td>{l.status}</td><td>{l.isTest?'TEST':''}</td></tr>)}</tbody></table></div></div></section></div>}